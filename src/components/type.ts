import { type } from "os";

export type getData = {
  img: string;
  amount: number;
  asset: string;
  referral_earnings: number;
  state: string;
  type: string;
  chain: {
    img: string;
    name: string;
  };
  user: any;

  name: string;
};
